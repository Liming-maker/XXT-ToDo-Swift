<Window x:Class="HomeworkToDo.Wpf.Views.CourseSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:models="clr-namespace:HomeworkToDo.Core.Models;assembly=HomeworkToDo.Core"
        Title="课程显示设置"
        Width="460" Height="600"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ProgressBar Grid.Row="0" IsIndeterminate="True"
                     Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}"
                     Margin="0,0,0,8"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Folders}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:SelectableFolder}">
                        <Border Margin="0,4" Padding="8" CornerRadius="6"
                                BorderBrush="#E0E0E0" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="{Binding Name}"
                                           FontWeight="SemiBold" FontSize="14"
                                           Margin="0,0,0,4"/>

                                <ItemsControl ItemsSource="{Binding Courses}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate DataType="{x:Type models:SelectableCourse}">
                                            <CheckBox IsChecked="{Binding IsSelected}"
                                                      Margin="16,3,0,3">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="{Binding Name}" FontSize="13"/>
                                                    <TextBlock Text="{Binding Teacher, StringFormat=' — {0}'}"
                                                               FontSize="11" Foreground="Gray"
                                                               Margin="4,0,0,0"/>
                                                </StackPanel>
                                            </CheckBox>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="保存" Width="80" Height="30" Margin="0,0,8,0"
                    Command="{Binding SaveCommand}" IsDefault="True"/>
            <Button Content="关闭" Width="80" Height="30" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
